<%= render "header"%>

<h1 class="title-page">Manage RSVP Form</h1>
<div class="ml-2">
  <%= link_to "Back to events", events_path %>
  <br/>
</div>

<%= render "survey_form", :event => @event, :form_title => @form_title, :form_submission_link => @form_submission_link,
	:form_edit_link => @form_edit_link, :form_exists => @form_exists, :num_responses => @num_responses %>
	