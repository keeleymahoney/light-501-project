<%= form_with(model: contacts_industry) do |form| %>
  <% if contacts_industry.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(contacts_industry.errors.count, "error") %> prohibited this contacts_industry from being saved:</h2>

      <ul>
        <% contacts_industry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
