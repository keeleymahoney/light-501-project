<!DOCTYPE html>
<html>
  <head>
    <title>Light501Project</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav>
      <ul>
        <% if member_signed_in? %>
          <li><%= link_to 'Logout', destroy_member_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to 'Sign in with Google', member_google_oauth2_omniauth_authorize_path %></li>
        <% end %>
      </ul>
    </nav>

    <%= yield %>
  </body>

</html>
