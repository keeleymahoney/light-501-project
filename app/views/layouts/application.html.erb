<!DOCTYPE html>
<html>
<head>
  <title>Light501Project</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

  <style>
    .flash_alert { color: red; }
    .flash_notice { color: blue; }
    .flash_success { color: green; }
  </style>
</head>

<body>
  <!-- Header with Navigation -->
  <header class="bg-light text-dark p-3">
    <div class="container">
      <div class="d-flex justify-content-between align-items-center">
        <div>
          <img src="/path/to/your/logo.png" alt="Logo" style="height: 60px;">
        </div>
        <nav>
          <ul class="nav">
            <li class="nav-item"><a class="nav-link" href="<%= root_path %>">Home</a></li>
            <li class="nav-item"><a class="nav-link" href="<%= events_path %>">Events</a></li>
            <li class="nav-item"><a class="nav-link" href="<%= media_path %>">Media</a></li>
            <li class="nav-item"><a class="nav-link" href="<%= featured_path %>">Featured</a></li> <!-- Featured page link -->
            <li class="nav-item"><a class="nav-link" href="#">Positions</a></li> <!-- Add correct path -->
            <li class="nav-item"><a class="nav-link" href="#">Forms</a></li> <!-- Add correct path -->
            <li class="nav-item"><a class="nav-link" href="<%= new_member_session_path %>">Login</a></li>
          </ul>
        </nav>
      </div>
    </div>
  </header>

  <!-- Flash Messages -->
  <div class="container">
    <% if flash.any? %>
      <% flash.each do |key, value| %>
        <section class="flash flash_<%= key %>"><%= value.html_safe %></section>
      <% end %>
    <% end %>
  </div>

  <!-- Main Content Area -->
  <div class="container">
    <%= yield %>
  </div>

  <!-- Footer -->
  <footer class="bg-dark text-white py-5">
    <div class="container text-center">
      <div class="row">
        <div class="col-md-6">
          <h5>Get in touch</h5>
          <p>
            Email: <a href="mailto:info.boldrso@gmail.com" class="text-white">info.boldrso@gmail.com</a><br>
            LinkedIn: <a href="https://www.linkedin.com/company/boldrso" class="text-white">BOLD on LinkedIn</a>
          </p>
        </div>
        <div class="col-md-6">
          <h5>Message this organization</h5>
          <p><a href="/login" class="text-white">Login to send this organization a message</a></p>
        </div>
      </div>
      <div class="text-muted mt-4">2024 Texas A&M University</div>
    </div>
  </footer>

</body>
</html>
