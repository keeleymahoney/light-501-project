<!DOCTYPE html>
<html>
  <head>
    <title>Light501Project</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <!-- Flash messages section -->
    <div class="flash-messages">
      <% flash.each do |key, message| %>
        <div class="alert alert-<%= key %>"><%= message %></div>
      <% end %>
    </div>

    <!-- Navigation links for signing in and signing out -->
    <nav>
      <% if member_signed_in? %>
        <p>
          Signed in as <%= current_member.email %>. 
          <%= link_to "Sign out", destroy_member_session_path, method: :delete %>
        </p>
      <% else %>
        <%= link_to "Sign in with Google", member_google_oauth2_omniauth_authorize_path %>
      <% end %>
    </nav>

    <!-- Yield to render views -->
    <%= yield %>

  </body>
</html>
